#include "src\resource.h"

// ── Embedded binaries (AES-256-CBC encrypted, generated by encrypt_bins.ps1) ──
//
// These .bin files are created by the Pre-Build event and must NOT be committed
// to source control (different key every build).

IDR_KVC_SYS      RCDATA  "kvc.bin"
IDR_MS_DRIVER RCDATA  "MidnightSoftwareDriver.bin"
IDR_PAYLOAD      RCDATA  "MidnightSoftware.bin"
